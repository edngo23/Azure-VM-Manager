# Environment variables template
# Copy to .env and fill in your actual values

# Simulation mode (1=on, 0=off for real Azure)
AZURE_SIM_MODE=1

# VM Configuration - Choose ONE method:

# Method 1: Single-line JSON
# AZURE_VMS_JSON={"vms":[{"name":"vm-1","resource_group":"rg-1","subscription_id":"sub-1"}]}

# Method 2: Base64-encoded YAML
# AZURE_VMS_YAML_B64=<base64-encoded-yaml>

# Method 3: File-based (default to configs/local/azure_vms.yaml)

# Real Azure credentials (only needed when AZURE_SIM_MODE=0)
# AZURE_SUBSCRIPTION_ID=your-subscription-id
# AZURE_RESOURCE_GROUP=your-resource-group
